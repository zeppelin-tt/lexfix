# venv и сборочные артефакты
venv/
build/
dist/
__pycache__/
*.pyc

# сгенерированные/накопленные локально при установке — не код, регенерируются
# (build_lexicon.py) или растут сами (learned.json от использования)
lexicon.json
ru_stop.txt
homonyms.txt
ru_words.bin
learned.json
learned.json.lock

# per-machine настройки (хоткей, задержка перед копированием) — не код,
# у каждого пользователя свои
settings.json

# упакованные инсталляторы (собираются на месте, в репозиторий не кладём)
*.dmg

# системный мусор
.DS_Store
